<UserControl
    x:Class="MoneyManagerAssistance.SubViews.More"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoneyManagerAssistance.SubViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
       <Style TargetType="TextBlock" x:Key="ThisTxtStyle">
           <Setter Property="FontSize" Value="25"></Setter>
           <Setter Property="Foreground" Value="{StaticResource AppForegroundColorBrush}"></Setter>
           <Setter Property="Margin" Value="0 10 0 10"></Setter>
       </Style>
    </UserControl.Resources>
    <Grid>
        <ListView x:Name="MoreListView">
            <ListView.Items>
                <TextBlock Text="设置" Style="{StaticResource ThisTxtStyle}" Margin="0"></TextBlock>
                <TextBlock Text="帮助"  Style="{StaticResource ThisTxtStyle}"></TextBlock>
                <TextBlock Text="好评应用"  Style="{StaticResource ThisTxtStyle}"></TextBlock>
                <TextBlock Text="关于"  Style="{StaticResource ThisTxtStyle}"></TextBlock>
            </ListView.Items>
        </ListView>
    </Grid>
</UserControl>
